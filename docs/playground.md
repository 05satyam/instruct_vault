# Playground (optional)

The playground is a separate package intended for teams that want a web UI to:
- edit prompts
- preview renders
- run evals
- open PRs for governed changes

It is not part of the core library to keep the runtime lightweight and auditable.

